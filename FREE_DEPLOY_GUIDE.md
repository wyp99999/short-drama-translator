# 🆓 短剧翻译平台免费部署指南

## 📋 方案概述
- **前端**: GitHub Pages (完全免费)
- **后端**: Vercel Serverless Functions (每月100GB流量免费)
- **数据库**: 内存数据库 (Demo 用，无需配置)
- **总成本**: 0元/月

## 🚀 快速部署步骤

### 第一步：准备代码

1. **克隆或复制项目**到您的 GitHub 仓库
2. **前端代码**在 `frontend/` 目录
3. **后端 API**在 `vercel-serverless/` 目录

### 第二步：部署后端 API (Vercel)

1. **访问 Vercel**: https://vercel.com
2. **注册账号** (支持 GitHub 登录)
3. **创建新项目**:
   - 点击 "New Project"
   - 导入您的 GitHub 仓库
   - 选择 `vercel-serverless` 目录
   - 点击 "Deploy"

4. **等待部署完成** (约1-2分钟)
5. **获取 API 地址**: 部署完成后会显示 `https://xxx.vercel.app`

### 第三步：部署前端 (GitHub Pages)

1. **构建前端**:
   ```bash
   cd frontend
   npm install
   npm run build
   ```

2. **修改 API 地址**:
   在 `.env.production` 中设置:
   ```
   VITE_API_URL=https://你的-vercel-app.vercel.app/api
   ```

3. **重新构建**:
   ```bash
   npm run build
   ```

4. **部署到 GitHub Pages**:
   - 将 `dist/` 目录内容推送到 GitHub
   - 在仓库设置中启用 GitHub Pages
   - 选择 `main` 分支的 `/dist` 目录

### 第四步：配置自定义域名 (可选)

1. **购买域名** (如: short-drama.yourname.com)
2. **在 Vercel 配置域名**:
   - 项目设置 → Domains
   - 添加您的域名
3. **在 GitHub Pages 配置域名**:
   - 仓库设置 → Pages → Custom domain
   - 添加您的域名

## 📁 项目结构

```
short-drama-translator/
├── frontend/                 # Vue3 前端
│   ├── src/
│   ├── public/
│   ├── .env.production      # 生产环境配置
│   └── vite.config.js
├── vercel-serverless/       # Serverless 后端
│   ├── api/
│   │   └── index.js        # 所有 API 路由
│   ├── package.json
│   └── vercel.json
└── FREE_DEPLOY_GUIDE.md    # 本指南
```

## 🔧 API 接口

部署后可用接口:

```
GET    /health                    # 健康检查
GET    /api/projects              # 获取项目列表
POST   /api/projects              # 创建项目
GET    /api/projects/:id          # 获取项目详情
PUT    /api/projects/:id          # 更新项目
DELETE /api/projects/:id          # 删除项目
GET    /api/projects/:id/status   # 获取任务状态
GET    /api/tasks/poll            # 任务轮询 (AI服务)
POST   /api/tasks/:id/complete    # 任务完成 (AI服务)
```

## 💡 功能特点

### ✅ 已实现功能
- 项目创建和管理
- 多语言选择 (10种语言)
- 实时任务状态跟踪
- 模拟翻译进度
- 响应式设计

### ⚠️ Demo 限制
- 内存数据库 (重启后数据丢失)
- 模拟视频翻译 (无实际AI处理)
- 无用户认证系统
- 无文件上传 (使用URL代替)

## 🎯 访问地址

部署完成后:
- **前端**: `https://你的用户名.github.io/仓库名` 或 自定义域名
- **后端**: `https://你的-vercel-app.vercel.app`
- **API文档**: `https://你的-vercel-app.vercel.app/health`

## 🔄 更新部署

### 更新后端:
```bash
cd vercel-serverless
# 修改代码后
vercel --prod
```

### 更新前端:
```bash
cd frontend
npm run build
# 将 dist/ 推送到 GitHub
```

## 📞 技术支持

### 常见问题:

1. **CORS 错误**
   - 检查前端 `.env.production` 中的 API 地址
   - 确保 Vercel 允许跨域

2. **GitHub Pages 404**
   - 检查仓库设置中的 Pages 配置
   - 确保 `dist/` 目录存在且正确

3. **Vercel 部署失败**
   - 检查 `vercel.json` 配置
   - 查看部署日志

### 获取帮助:
- Vercel 文档: https://vercel.com/docs
- GitHub Pages 文档: https://docs.github.com/pages
- Vue3 文档: https://vuejs.org

## 🎉 开始使用

1. 访问您的前端地址
2. 创建新项目
3. 选择目标语言
4. 查看模拟翻译进度
5. 完成 Demo 演示！

---

**注意**: 这是 Demo 版本，适合演示和测试。生产环境需要添加数据库、文件存储、用户认证等功能。